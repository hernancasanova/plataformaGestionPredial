--CREATE USER "HERNAN" IDENTIFIED BY "Hernamstein3" DEFAULT TABLESPACE "USERS" QUOTA UNLIMITED ON "USERS" ; 
 /* CREATE TABLE "HERNAN"."DOCUMENTS" 
   (	"ID" NUMBER(38) GENERATED BY DEFAULT AS IDENTITY, 
	"NAME" VARCHAR2(100), 
	"DESCRIPTION" VARCHAR2(100), 
	"TYPE" NUMBER(38),
    PRIMARY KEY("ID")
   );*/

CREATE USER "HERNAN" IDENTIFIED BY "Hernamstein3" DEFAULT TABLESPACE "USERS" QUOTA UNLIMITED ON "USERS" ; 
ALTER SESSION SET CURRENT_SCHEMA="HERNAN";

CREATE SEQUENCE sequence_document
MINVALUE 0
START WITH 0
INCREMENT BY 1 NOCACHE NOCYCLE;

CREATE TABLE "DOCUMENTS" (
	ID INTEGER,
	NAME VARCHAR2(100),
	DESCRIPTION VARCHAR2(100),
	TYPE INTEGER,
	CONSTRAINT DOCUMENTS_PK PRIMARY KEY (ID) 
);

INSERT INTO "DOCUMENTS" (ID,NAME,DESCRIPTION,TYPE) values (sequence_document.nextval,'Movimiento animal 2','Formulario emitido por el SAG para el traslado de animales','1');

CREATE TRIGGER DOCUMENT_TRIGGER_3
BEFORE INSERT ON "DOCUMENTS"
FOR EACH ROW
BEGIN
  SELECT sequence_document.nextval
  INTO :new.ID
  FROM dual;
END;
